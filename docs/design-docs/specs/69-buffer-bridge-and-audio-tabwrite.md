# 69. Buffer Bridge and Audio Tabwrite~

TBA

- Use `COEP: credentialless`
  - credential isolation
- Add header on Cloudflare / SvelteKit
- `SharedArrayBuffer`
- iframes work, just credentialless

```txt
Cross-Origin-Opener-Policy: same-origin
Cross-Origin-Embedder-Policy: credentialless
```
