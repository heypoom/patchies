Displays the latest received value, useful for debugging message flow.

Shows strings, numbers, booleans, and JSON objects with formatting.

## Expression Mode

Optional expression to peek at specific fields: `peek $1.type` or click the
code icon to add an expression.

Use `$1` to reference the incoming message (e.g., `$1.x`, `$1.data.name`).

## See Also

- [tap](/docs/objects/tap) - debug with side effects
- [msg](/docs/objects/msg) - display and send messages
