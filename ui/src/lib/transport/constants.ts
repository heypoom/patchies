/** Default beats per minute */
export const DEFAULT_BPM = 120;

/** Pulses per quarter note (standard MIDI resolution) */
export const DEFAULT_PPQ = 192;

/** Default time signature as [numerator, denominator] */
export const DEFAULT_TIME_SIGNATURE: [number, number] = [4, 4];

/** Whether transport auto-plays on startup (better for onboarding?) */
export const DEFAULT_AUTOPLAY = true;
