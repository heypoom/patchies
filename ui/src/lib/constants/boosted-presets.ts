/**
 * Presets that should be boosted above objects with similar names in search results.
 * For example, "logger.js" should appear before "log~" when typing "log".
 */
export const BOOSTED_PRESETS = new Set(['logger.js']);
