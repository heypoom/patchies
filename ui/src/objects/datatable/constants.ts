import type { DatatableData } from './datatable-utils';

export const DEFAULT_DATATABLE_DATA: DatatableData = {
  columns: ['column 1', 'column 2'],
  rows: [
    ['', ''],
    ['', '']
  ],
  outputObjects: false
};
